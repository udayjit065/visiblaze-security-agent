Binaries and build artifacts

These are generated by your build process

*.exe
*.deb
*.rpm
*.zip
bootstrap
visiblaze-agent
dist/
bin/

Go module vendor directory

This folder is managed by 'go mod' and shouldn't be in Git

vendor/

Terraform state files

These files contain sensitive info about your infrastructure

*.tfstate
*.tfstate.backup
*.tfplan

Terraform variables

CRITICAL: This file often contains secret keys!

terraform.tfvars

Terraform internal files

.terraform/
.terraform.lock.hcl

Server access keys

CRITICAL: Never commit your private keys!

*.pem

OS junk files

.DS_Store
Thumbs.db